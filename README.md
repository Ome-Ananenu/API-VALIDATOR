# API Validator

A simple Node.js tool for QA engineers to validate API responses.

## Features
- Check if API returns the expected status code
- Check if a JSON key exists in the response

## Installation
```bash
git clone https://github.com/Ome-Ananenu/api-validator.git
cd api-validator
npm install


